import { PageShell } from '@/components/page-shell';
export default function SellerSettingsPage() { return <PageShell title='Shop Settings & Subscription' description='Profile, verification, plan details, renewal, usage limits.' />; }
