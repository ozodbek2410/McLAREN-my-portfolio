import { PageShell } from '@/components/page-shell';
export default function SellerOrdersPage() { return <PageShell title='Seller Orders' description='Incoming orders and status updates from pending to delivered.' />; }
