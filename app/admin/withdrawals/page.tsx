import { PageShell } from '@/components/shared/page-shell';

export default function AdminWithdrawalsPage() {
  return <PageShell title="Withdrawals" description="Approve seller payout requests with transfer notes and audit trail." />;
}
