import { PageShell } from '@/components/shared/page-shell';

export default function SellerAnalyticsPage() {
  return <PageShell title="Seller Analytics" description="Monthly chart, top products, conversion rates, and subscription insights." />;
}
