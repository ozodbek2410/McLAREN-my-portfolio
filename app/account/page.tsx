import { PageShell } from '@/components/page-shell';
export default function AccountPage() { return <PageShell title='Buyer Account' description='Orders, wishlist, addresses, reviews and profile settings.' />; }
