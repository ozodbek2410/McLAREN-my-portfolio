import { PageShell } from '@/components/page-shell';

export default function ProductsPage() {
  return <PageShell title='Product Listing' description='Category tree filters, full-text search, rating + price filters, sort options.' />;
}
